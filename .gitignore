*.iml
.gradle/
local.properties
# 正式签名凭据：引用仓库外的密钥库，绝不入库
keystore.properties
*.jks
*.keystore
.idea/
.DS_Store
build/
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab

# AI 助手工作目录（会话记忆 / 中间产物，不入库）
.workbuddy/
.workbuddy-ai/

# Gradle 本地守护进程 JVM 配置（因机器而异）
gradle/gradle-daemon-jvm.properties

# 🔐 签名密钥与凭据 —— 绝不入库
# 密钥一旦泄露，任何人都能签发可覆盖你已上架应用的更新包。
keystore.properties
*.jks
*.keystore
keystore/

# 发布产物（体积大，按需重新构建）；但保留发布指南这一份文档
release/*
!release/发布指南.md

# 图表校验副产物（截图与回执，非交付物）
*.visual-check.json
*.visual-check.html
*.visual-check.*.png

# 品牌/商店素材：本地保留即可，不参与构建也无人引用，不必进公开仓库
app/HomeHealthLogo.png
app/src/main/ic_launcher-playstore.png
